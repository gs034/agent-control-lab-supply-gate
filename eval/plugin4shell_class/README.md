# Plugin4Shell-class (threat pattern)

Existence-proof DENY only. Not an attack-success-rate claim. Not a vendor product.

A marketplace/plugin install may satisfy a naive SHA pin on a mutable ref
while the materialised tree has moved. This row checks post-checkout HEAD
against the pin and ignores marketplace prose as policy.
