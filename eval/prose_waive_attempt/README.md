# Prose-waive attempt

Existence-proof DENY only. Not an attack-success-rate claim.

A structured waive (`skip_verify`) plus marketplace prose cannot skip
post-checkout HEAD verify. Even when HEAD matches the pin, the host
path DENY with `prose_rejected_as_policy` and `verify_performed` stays
true.
