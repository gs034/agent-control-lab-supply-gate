# auto_latest rejected

Existence-proof DENY only. Not an attack-success-rate claim.

A weak update mode (`auto_latest`) cannot substitute for pin-and-verify.
The host path DENY with `update_policy_rejected` even when HEAD matches.
