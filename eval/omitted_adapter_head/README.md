# Omitted adapter HEAD

Existence-proof DENY only. Not an attack-success-rate claim.

The installer adapter omitted post-checkout HEAD. The host path still
calls `evaluate`; missing HEAD is `verify_missing`.
