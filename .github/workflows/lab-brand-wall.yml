# SPDX-License-Identifier: Apache-2.0
name: lab-brand-wall

on:
  pull_request:
  push:

jobs:
  wall:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Lab-only brand wall (stdlib; no ripgrep)
        run: PATH="/usr/bin:/bin" python3 scripts/lab_brand_wall.py
